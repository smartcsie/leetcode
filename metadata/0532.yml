number: 532
title: K-diff Pairs in an Array
url: https://leetcode.com/problems/k-diff-pairs-in-an-array/
solutions:
- file: Solution_0532.cpp
  difficulty: 🟡 Medium
  tags:
  - Hash Table Existence
  topics:
  - hash-table-existence
  time: O(NlogN) - 排序後雙指針或二分搜尋。
  space: O(N) - 排序空間 O(logN)，set O(N)。
  familiarity: 再練習
  note: '> 兩種做法<br>

    > 或是存數對的前面<br>

    > if(seen.count(x - k))  ans.insert(x - k);<br>

    > if(seen.count(x + k))  ans.insert(x);<br>

    > 或是存數對的後面<br>

    > if(seen.count(x - k))  ans.insert(x);<br>

    > if(seen.count(x + k))  ans.insert(x + k);<br>'
