number: 2553
title: Separate the Digits in an Array
url: https://leetcode.com/problems/separate-the-digits-in-an-array
solutions:
- file: Solution_2553.cpp
  difficulty: 🟢 Easy
  tags:
  - Array
  - Math
  topics:
  - math-digit-decomposition
  time: O(N*log(max_VAL))
  space: O(N*log(max_VAL))
  note: '> [Separate the Digits in an Array](https://leetcode.com/problems/separate-the-digits-in-an-array)
    <br>

    > 方法一<br>

    > **for(int i = nums.size() - 1 ; i >= 0; i--)** {<br>

    > &emsp;&emsp;&emsp;&emsp;**int temp = nums[i];** <br>

    > &emsp;&emsp;&emsp;&emsp;**while(temp > 0) {** {<br>

    > &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;**res.push_back(temp % 10);**
    {<br>

    > &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;**temp /= 10;** {<br>

    > &emsp;&emsp;&emsp;&emsp;**}** {<br>

    > **}<br>

    > reverse(res.begin(), res.end());<br>

    > 方法二<br>

    > **for(const int& num : nums) {** <br>

    > &emsp;&emsp;&emsp;&emsp;**for(char c : to_string(num)) res.push_back(c - ''0'');**
    <br>

    > **}** <br>'
