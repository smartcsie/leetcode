number: 50
title: Pow(x, n)
url: https://leetcode.com/problems/powx-n
solutions:
- file: Solution_0050.cpp
  difficulty: 🟡 Medium
  tags:
  - Math
  - Binary Exponentiation
  topics:
  - math
  time: O(logN)
  space: O(1)
  note: '> [Pow(x, n)](https://leetcode.com/problems/powx-n)<br>

    > **if (n == 0) return 1;** <br>

    > **if (n == INT_MIN) return myPow(x * x, n / 2);** <br>

    > **if (n < 0) return 1 / myPow(x, -n);** <br>

    > **double half = myPow(x, n / 2);** <br>

    > **return n % 2 == 0 ? half * half : half * half * x;** <br>

    > **overflow handing** <br>'
