# 1929. Concatenation of Array

[在 LeetCode 上查看](https://leetcode.com/problems/concatenation-of-array)

## 解法：array-linear-scan

**難度:** 🟢 Easy　**標籤:** Array Linear Scan　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練　🏆 **最佳解**　👑 **代表題（vector insert）**

**📅 練習歷程:**

- ❌ **2026-09-14** 錯
    class Solution {
    public:
        vector<int> getConcatenation(vector<int>& nums) {
            nums.insert(nums.begin(), nums);
            return nums;
        }
    };
- ✅ **2026-09-15** 對

**技巧:** insert

```cpp
/**
 * 題目：1929. Concatenation of Array (串聯陣列)
 * 難度：簡單 (Easy)
 * 描述：返回一個長度為 2n 的陣列，其中 ans[i] == nums[i] 且 ans[i + n] == nums[i]。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(N)
 *
 * 解法思路：
 * 1. 使用 reserve 預先分配記憶體是正確的優化。
 * 2. 也可以直接使用 vector 的建構子或是 insert 語法，這在內部執行上通常會被優化為 memmove。
 */

#include <vector>

using namespace std;

class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        nums.insert(nums.end(), nums.begin(), nums.end());
        return nums;
    }
};
```

**相關題目:** [3925](../problems/3925.md)

---

## 解法：array-linear-scan

**難度:** 🟢 Easy　**標籤:** Array Linear Scan　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練　👑 **代表題（vector resize）**

**📅 練習歷程:**

- ✅ **2026-09-15** 對

**技巧:** resize

```cpp
class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        int n = nums.size();
        nums.resize(n * 2);
        for(int i = 0; i < n; i++) {
            nums[i+ n] = nums[i];
        }
        return nums;
    }
};
```

---
