# 2114. Maximum Number of Words Found in Sentences

[在 LeetCode 上查看](https://leetcode.com/problems/maximum-number-of-words-found-in-sentences)

**難度:** 🟢 Easy　**標籤:** String　**時間:** O(N*M)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-20** 對

**技巧:** count

```cpp
/**
 * 題目：2114. Maximum Number of Words Found in Sentences (句子中的最多單詞數)
 * 難度：簡單 (Easy)
 * 描述：計算多個句子中包含單詞數量的最大值。
 *
 * 時間複雜度：O(N*M)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 根據題目，單詞間由單個空格分隔。因此，單詞數 = 空格數 + 1。
 * 直接遍歷字串統計空格，避免使用 `stringstream` 造成的記憶體配置與格式化開銷。
 */

class Solution {
public:
    int mostWordsFound(vector<string>& sentences) {
        int mx = 0;
        for(const string& s : sentences) {
            int segments = count(s.begin(), s.end(), ' ') + 1;
            mx = max(mx, segments);
        }
        return mx;
    }
};
```
