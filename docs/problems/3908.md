# 3908. Valid Digit Number

[在 LeetCode 上查看](https://leetcode.com/problems/valid-digit-number/)

**難度:** 🟢 Easy　**標籤:** Math, Digit Manipulation　**時間:** O(logN)　**空間:** O(1)　**熟悉度:** 🔴 生疏

```cpp
/**
 * 題目：3908. Valid Digit Number (有效的位數數字)
 * 難度：簡單 (Easy)
 * 描述：判斷整數 n 的最高位是否不為 x，且剩餘位數中是否至少包含一個 x。
 *
 * 時間複雜度：O(logN)，遍歷數字的每一位。
 * 空間複雜度：O(1)，僅使用幾個整數變數。
 *
 * 解法思路：
 * 1. 移除浮點數運算：避免 `log10` 和 `floor` 的精度風險，改用純整數除法。
 * 2. 單次遍歷：在提取位數的同時同步記錄最高位與是否出現過 x。
 */

class Solution {
public:
    bool validDigit(int n, int x) {
        if(n <= 0) return false;
        int msb = -1;
        bool occur = false;
        while(n > 0) {
            int digit = n%10;
            if(digit == x) occur = true;
            msb = digit;
            n /= 10;
        }
        return (msb != x) && occur;
    }
};
```

> [Valid Digit Number](https://leetcode.com/problems/valid-digit-number/) <br>
> 用一個bool紀錄digit是否有x，最後return digit != x && findX; <br>
