# 1317. Convert Integer to the Sum of Two No-Zero Integers

[在 LeetCode 上查看](https://leetcode.com/problems/convert-integer-to-the-sum-of-two-no-zero-integers/)

**難度:** 🟢 Easy　**標籤:** Math Digit Decomposition, Simulation　**時間:** O(N*logN)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-23** 對

```cpp
/**
 * 題目：1317. Convert Integer to the Sum of Two No-Zero Integers
 * 難度：中等 (Medium)
 * 描述：給定一個整數 n，將其表示為兩個「不包含數字 0 的正整數」之和。
 * 若有多個可能解，返回其中任意一個即可。題目保證一定存在至少一個解。
 *
 * 時間複雜度：O(N*logN)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 1. 輔助函式 hasZero(int n)：透過取餘數與除以 10 (n % 10, n / 10)，
 * 檢查一個數字的十進位表示中是否包含數字 0。
 * 2. 主邏輯 getNoZeroIntegers(int n)：
 * - 從 i = 1 開始迴圈遞增到 n - 1。
 * - 每次檢查 i 與 n - i 是否都不包含 0（即 !hasZero(i) && !hasZero(n - i)）。
 * - 若雙雙符合條件，立即回傳 {i, n - i}。
 */

class Solution {
private:
    bool noZero(int n) {
        while(n > 0) {
            if(n % 10 == 0) return false;
            n /= 10;
        }
        return true;
    }
public:
    vector<int> getNoZeroIntegers(int n) {
        for(int x = 1; x < n / 2; x++) {
            if(noZero(x) && noZero(n - x)) return {x, n - x};
        }
        return {-1, -1};
    }
};
```
