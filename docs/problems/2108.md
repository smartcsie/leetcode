# 2108. Find First Palindromic String in the Array

[在 LeetCode 上查看](https://leetcode.com/problems/find-first-palindromic-string-in-the-array/)

**難度:** 🟢 Easy　**標籤:** Two Pointers　**時間:** O(N*L)　**空間:** O(1)　**熟悉度:** 🟢 熟練

```cpp
/**
 * 註解：待補充
 */
**
 * 題目：2108. Find First Palindromic String in the Array
 * 難度：簡單 (Easy)
 * 描述：找出陣列中第一個回文字串。
 * * 優化重點：
 * 1. 將回文檢查邏輯內嵌，減少函式呼叫開銷。
 * 2. 使用 std::equal 搭配 reverse_iterator，達到更簡潔且高效的檢查。
 * 時間複雜度：O(N*L) - 對每個字串做回文檢查，L 為字串長度。
 * 空間複雜度：O(1) - 僅使用常數空間。

 */

class Solution {
private:
    bool isPalindrome(const string& word) {
        int left = 0;
        int right = word.size() - 1;
        while(left < right) {
            if(word[left++] != word[right--]) return false;
        }
        return true;
    }
public:
    string firstPalindrome(vector<string>& words) {
        for(const string& word : words) {
            if(isPalindrome(word)) return word;
        }
        return "";
    }
};
```

> 實做string isPalindromic，travsal vector<string>& words，第一個Palindromic回傳<br>
<br><br>
