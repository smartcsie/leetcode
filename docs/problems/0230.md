# 230. Kth Smallest Element in a BST

[在 LeetCode 上查看](https://leetcode.com/problems/kth-smallest-element-in-a-bst)

**難度:** 🟡 Medium　**標籤:** Tree, DFS, BST　**時間:** O(K)　**空間:** O(H)　**熟悉度:** 🟠 再練習

```cpp
/**
 * 題目：230. Kth Smallest Element in a BST (BST 中第 K 小的元素)
 * 難度：中等 (Medium)
 * 描述：待補充
 *
 * 時間複雜度：O(H+K)
 * 空間複雜度：O(H)
 */

class Solution {
private:
    void inorder(TreeNode* root, int& k, int& ans) {
        if(!root) return;
        inorder(root->left, k, ans);
        k--;
        if(k == 0) {
            ans = root->val;
            return;
        }
        inorder(root->right, k, ans);
    }
public:
    int kthSmallest(TreeNode* root, int k) {
        int ans;
        inorder(root, k, ans);
        return ans;
    }
};


/**  更好的寫法
int kthSmallest(TreeNode* root, int k) {
    stack<TreeNode*> s;
    TreeNode* curr = root;

    while (curr || !s.empty()) {
        // 一直往左走到底
        while (curr) {
            s.push(curr);
            curr = curr->left;
        }
        
        // 彈出棧頂節點（目前最小的）
        curr = s.top();
        s.pop();
        
        // 計數
        if (--k == 0) return curr->val;
        
        // 轉向右子樹
        curr = curr->right;
    }
    return -1;
}
*/
```

> k是call by reference <br>
> int& k <br>
