# 2089. Find Target Indices After Sorting Array

[在 LeetCode 上查看](https://leetcode.com/problems/find-target-indices-after-sorting-array/)

**難度:** 🟢 Easy　**標籤:** Array Linear Scan, Counting　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練　👑 **代表題（vector iota）**

**📅 練習歷程:**

- ✅ **2026-09-15** 對

**技巧:** iota

```cpp
/**
 * 題目：2089. Find Target Indices After Sorting Array (排序後目標值的索引)
 * 難度：簡單 (Easy)
 * 描述：排序陣列後，回傳所有目標值 target 的索引。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 1. 既然要找排序後的索引，我們不需要真正執行排序。
 * 2. 只需要統計：
 * - 小於 target 的數量 (作為起始索引)。
 * - 等於 target 的數量 (決定區間長度)。
 */

class Solution {
public:
    vector<int> targetIndices(vector<int>& nums, int target) {
        int smaller = 0;
        int count = 0;
        for(const int& x : nums) {
            if(x == target) count++;
            else if(x < target) smaller++;
        }
        vector<int> ans(count);
        iota(ans.begin(), ans.end(), smaller);
        return ans;
    }
};
```
