# 2395. Find Subarrays With Equal Sum

[在 LeetCode 上查看](https://leetcode.com/problems/find-subarrays-with-equal-sum/)

**難度:** 🟢 Easy　**標籤:** Hash Table Existence, Hash Table　**時間:** O(N)　**空間:** O(N)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-16** 對
- ✅ **2026-09-16** 對

**技巧:** int count

```cpp
/**
 * 題目：2395. Find Subarrays With Equal Sum (找出和相等的子陣列)
 * 難度：簡單 (Easy)
 * 描述：檢查陣列中是否存在兩個長度為 2 的子陣列，其元素和相等。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(N)
 *
 * 解法思路：
 * 1. 記憶體預留：使用 `reserve` 預先配置空間，提升雜湊表運算效率。
 * 2. 減少條件分支：利用 `unordered_set::insert` 的回傳值特性，進一步簡化判斷。
 */

class Solution {
public:
    bool findSubarrays(vector<int>& nums) {
        unordered_map<int, int> counts;
        for(int i = 1; i <nums.size(); i++) {
            int sum = nums[i] + nums[i - 1];
            counts[sum]++;
            if(counts[sum] == 2) return true;
        }
        return false;
    }
};
```
