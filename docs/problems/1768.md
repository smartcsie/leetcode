# 1768. Merge Strings Alternately

[在 LeetCode 上查看](https://leetcode.com/problems/merge-strings-alternately)

**難度:** 🟢 Easy　**標籤:** Two Pointers Same, Two Pointers　**時間:** O(N+M)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-18** 對

```cpp
/**
 * 題目：1768. Merge Strings Alternately (交替合併字串)
 * 難度：簡單 (Easy)
 * 描述：交替合併 word1 和 word2，若某字串較長，則將剩餘部分接在後面。
 *
 * 時間複雜度：O(N+M)
 * 空間複雜度：O(1)
 */

class Solution {
public:
    string mergeAlternately(string word1, string word2) {
        string ans, &w1 = word1, & w2 = word2;
        int m = w1.size(), n = w2.size();
        ans.reserve(m + n);
        for(int i = 0; i <max(m, n); i++) {
            if(i < m) ans.push_back(w1[i]);
            if(i < n) ans.push_back(w2[i]);
        }
        return ans;
    }
};
```

> 共用一個 i
