# 2716. Minimize String Length

[在 LeetCode 上查看](https://leetcode.com/problems/minimize-string-length/)

## 解法：bit-manipulation

**難度:** 🟢 Easy　**標籤:** Bit Manipulation, Hash Set　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-17** 對
- ✅ **2026-09-17** 對

```cpp
/**
 * 題目：2716. Minimize String Length (最小化字串長度)
 * 難度：簡單 (Easy)
 * 描述：透過選擇兩個索引 i 和 j，若 s[i] == s[j]，可移除其中一個。重複操作直到無法再移除。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 1. 唯一性：題目本質是求「不同字元的數量」。
 * 2. Bit Manipulation：使用 bitset<26> 高效記錄出現過的字元，並直接計算已設位元的總數。
 */

class Solution {
public:
    int minimizedStringLength(string s) {
        bitset<26> bits;
        for(const char& c : s) {
            bits.set(c - 'a');
        }
        return static_cast<int>(bits.count());
    }
};
```

> 就是計算相異字元個數，用bitset<26> bits紀錄，最後回傳 bits.count()<br>

**相關題目:** [1832](../problems/1832.md)

---

## 解法：bit-manipulation

**難度:** 🟢 Easy　**標籤:** Bit Manipulation　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-17** 對
- ✅ **2026-09-17** 對

```cpp
/**
 * 題目：2716. Minimize String Length
 * 難度：簡單 (Easy)
 * 分類主題：bit-manipulation-mask
 * 描述：給定一個字串 s，每次操作可以選擇一個「至少出現兩次」的字元，
 * 刪掉它的其中一次出現。可以做任意次操作，求最後能得到的最短字串
 * 長度。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * （Bitmask，這題的關鍵洞察是：不管字元出現幾次，最終都能被反覆刪到
 * 只剩一個——所以答案就是「字串裡有幾種不同的字元」，跟每個字元
 * 各自出現的次數完全無關）：
 * 1. **關鍵觀察**：只要一個字元出現次數 >= 2，就可以一直刪到剩 1 個
 *    （每次操作把出現次數減 1，重複操作到剩 1 個為止）。所以每種
 *    字元最終都會剩下恰好 1 個，答案 = 字串裡不同字元的種類數。
 * 2. `seen` 是一個 int 遮罩，用 `seen |= 1 << (c - 'a')` 把每個出現
 *    過的字元對應的 bit 設成 1——這是最基本的 bitmask 去重技巧，
 *    不管某個字元出現幾次，同一個 bit 只會被設定、不會重複累加。
 * 3. `__builtin_popcount(seen)` 直接數出 `seen` 裡有幾個 bit 是 1，
 *    就是字串裡不同字元的種類數，也就是答案。
 * 4. 這題完全不需要真的模擬刪除操作、也不需要統計每個字元的出現
 *    次數，想通「答案只跟字元種類數有關」這個洞察之後，程式碼變得
 *    非常簡短。
 */
class Solution {
public:
    int minimizedStringLength(string s) {
        int seen = 0;
        for(const char& c : s) seen |= 1 << (c - 'a');
        return __builtin_popcount(seen);
    }
};
```

---
