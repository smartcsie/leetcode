# 3158. Find the XOR of Numbers Which Appear Twice

[在 LeetCode 上查看](https://leetcode.com/problems/find-the-xor-of-numbers-which-appear-twice/)

**難度:** 🟢 Easy　**標籤:** Bit Manipulation Xor, Bit Manipulation　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-18** 對

```cpp
/**
 * 題目：3158. Find the XOR of Numbers Which Appear Twice (找出出現兩次數字的 XOR 值)
 * 難度：簡單 (Easy)
 * 描述：在陣列中找出所有出現兩次的數字，並返回它們的 XOR 結果。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 1. 使用棧陣列 (int count[51]) 避免 vector 在堆上配置記憶體的開銷。
 * 2. 累加計數的同時判斷是否為第二次出現，減少後續遍歷次數。
 */

#include <vector>

class Solution {
public:
    int duplicateNumbersXOR(std::vector<int>& nums) {
        vector<int> counts(51, 0);
        for(const int& x : nums) counts[x]++;
        int ans = 0;
        for(int i = 1; i <= 50; i++) {
            if(counts[i] == 2) ans ^= i;
        }
        return ans;
    }
};
```

> 先travsal nums一次 counts紀錄出現次數<br>
> 從1 ~ 51 counts次數為2的 xor到ans
