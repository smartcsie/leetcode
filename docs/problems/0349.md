# 349. Intersection of Two Arrays

[在 LeetCode 上查看](https://leetcode.com/problems/intersection-of-two-arrays/)

**難度:** 🟢 Easy　**標籤:** Hash Table Intersection　**時間:** O(N+M)　**空間:** O(N)　**熟悉度:** 🟢 熟練　🏆 **最佳解**

```cpp
/**
 * 題目：349. Intersection of Two Arrays
 * 難度：簡單 (Easy)
 * 描述：找出兩個陣列的交集，結果中的每個元素只能出現一次。
 * 時間複雜度：O(N+M)
 * 空間複雜度：O(N)

 */
class Solution {
public:
    vector<int> intersection(vector<int>& nums1, vector<int>& nums2) {
        vector<int> counts1(1001, 0);
        vector<int> counts2(1002, 0);
        for(const int& num : nums1) {
            counts1[num] = 1;
        }
        for(const int& num : nums2) {
            counts2[num] = 1;
        }
        vector<int> ans;
        for(int i = 0; i <= 1000; i++) {
            if(counts1[i] && counts2[i]) ans.push_back(i);
        }
        return ans;
    }
};
```
