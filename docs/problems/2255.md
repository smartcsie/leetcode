# 2255. Count Prefixes of a Given String

[在 LeetCode 上查看](https://leetcode.com/problems/count-prefixes-of-a-given-string/)

**難度:** 🟢 Easy　**標籤:** String Character Classification　**時間:** O(N*M)　**空間:** O(1)　**熟悉度:** 🟢 熟練　👑 **代表題（String Prefix）**

**📅 練習歷程:**

- ✅ **2026-09-21** 對
- ✅ **2026-09-21** 對

```cpp
/**
 * 題目：2255. Count Prefixes of a Given String
 * 難度：簡單 (Easy)
 * 描述：計算 words 陣列中有多少單詞是字串 s 的前綴。
 *
 * 時間複雜度：O(N*M)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 1. 遍歷 words 中的每一個單詞。
 * 2. 使用 `string::find` 檢查該單詞是否在 s 的索引 0 處出現。
 * 3. 若 `find` 回傳 0，則表示該單詞是 s 的前綴。
 */

class Solution {
public:
    int countPrefixes(std::vector<std::string>& words, std::string s) {
        int count = 0;
        for (const std::string& word : words) {
            if (s.find(word) == 0) {
                count++;
            }
        }
        return count;
    }
};
```

> **for(const string& word : words) {**
> &emsp;&emsp;&emsp;&emsp**if(s.find(word) == 0) count++;**
> **}**

**相關題目:** [1455](../problems/1455.md), [2185](../problems/2185.md)
