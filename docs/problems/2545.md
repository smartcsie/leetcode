# 2545. Sort the Students by Their Kth Score

[在 LeetCode 上查看](https://leetcode.com/problems/sort-the-students-by-their-kth-score/)

**難度:** 🟡 Medium　**標籤:** Sorting Custom Comparator, Sorting　**時間:** O(NlogN)　**空間:** O(1)　**熟悉度:** 🟢 熟練

**📅 練習歷程:**

- ✅ **2026-09-26** 對

```cpp
/**
 * 題目：2545. Sort the Students by Their Kth Score (依照第 K 個分數排序學生)
 * 難度：中等 (Medium)
 * 描述：根據每個學生在第 k 個考試的分數，對所有學生進行從高到低的排序。
 *
 * 時間複雜度：O(M*NlogN)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 1. 簡化比較邏輯：直接比較 v1[k] 與 v2[k]，不需要額外的判斷式。
 * 2. 移除冗餘判斷：題目未要求在分數相同時處理次要排序，移除 v1 < v2 可提升效能。
 */

class Solution {
public:
    vector<vector<int>> sortTheStudents(vector<vector<int>>& score, int k) {
        sort(score.begin(), score.end(),[&](vector<int>& class1, vector<int>& class2) {
            return class1[k] > class2[k];
        });
        return score;
    }
};
```
