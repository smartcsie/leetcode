# 2710. Remove Trailing Zeros From a String

[在 LeetCode 上查看](https://leetcode.com/problems/remove-trailing-zeros-from-a-string/)

**難度:** 🟢 Easy　**標籤:** String Character Classification　**時間:** O(N)　**空間:** O(1)　**熟悉度:** 🟢 熟練　👑 **代表題（Remove Trailing）**

**📅 練習歷程:**

- ✅ **2026-09-20** 對
- ✅ **2026-09-21** 對

**技巧:** remove trailing

```cpp
/**
 * 題目：2710. Remove Trailing Zeros From a String
 * 難度：簡單 (Easy)
 * 描述：移除字串表示的整數末尾的所有零。
 *
 * 時間複雜度：O(N)
 * 空間複雜度：O(1)
 *
 * 解法思路：
 * 方案一 (Pop back)：使用 `pop_back()` 直接刪除末尾的 '0'，直到遇到非 '0' 字元。
 * 方案二 (Substr)：找到最後一個非零字元的位置，直接截取子字串。
 */


class Solution {
public:
    std::string removeTrailingZeros(std::string num) {
        while(!num.empty() && num.back() == '0') num.pop_back();
        return num;
    }
};
```

> 類似 3856[Trim Trailing Vowels](https://leetcode.com/problems/trim-trailing-vowels/) <br>
> **while (!num.empty() && num.back() == '0'){** <br>

**相關題目:** [1903](../problems/1903.md), [3798](../problems/3798.md), [3856](../problems/3856.md)
